# Orpheon

Two data scientists on quests.

## Preview

```bash
python3 -m http.server 8080
```

## GitHub Pages

Settings → Pages → Deploy from a branch → `main` / root.

Single-file site: `index.html`.
